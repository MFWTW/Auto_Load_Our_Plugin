/**
 * ui-workflow-runs — 右侧「工作流运行」面板（宿主空壳）
 *
 * 能力全部在浏览器半实现（见 ./client.js）；宿主侧保持可加载。
 * 数据来源：/api/workflow-runs（由 dsh-workflow-registry 提供）。
 */
export const name = 'ui-workflow-runs'

export function apply() {}
